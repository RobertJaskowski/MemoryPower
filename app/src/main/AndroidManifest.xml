<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rj.pl.memorypower">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NumbersIntro" />
        <activity android:name=".NumbersSession" />
        <activity android:name=".WordsIntro" />
        <activity android:name=".WordsSession" />
        <activity android:name=".NamesIntro" />
        <activity android:name=".NamesSession" />
        <activity android:name=".CardsIntro" />
        <activity android:name=".CardsSession"/>
    </application>

</manifest>